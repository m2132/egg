do(
    @set(format4, @fun(
        a, b, c, d, do(
            +(
                +(
                    +(
                        str(a),
                        str(b),
                    ),
                    str(c),
                )
                str(d)
            )
        )
    )),

    log(format4('A', 2, 'C', +(4, 2)))
)